# Web / ASGI server (choose one: Flask or FastAPI + Uvicorn)
flask==2.3.3
gunicorn==20.1.0

# OR (preferred for async/high-performance APIs):
fastapi==0.95.3
uvicorn==0.22.0

# Numerical / scientific (if you compute on server-side)
numpy==1.26.2
scipy==1.11.3

# Utility
python-multipart==0.0.6     # form/file parsing for FastAPI if you accept uploads
aiofiles==23.1.0            # async file serving helper

# Optional: for production deployments (recommended)
python-dotenv==1.0.0
